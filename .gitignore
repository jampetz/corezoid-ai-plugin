node_modules/
package-lock.json
.DS_Store
.envrc
.processes
.idea/
.vscode
convctl
yarn.lock
/plugins/simulator/mcp-server/info
.env
coverage.out
# Root-level only — plugins/corezoid/.mcp.json must stay tracked (it ships in the marketplace package).
# Do NOT change to **/.mcp.json.
/.mcp.json

# Python
__pycache__/
*.pyc
